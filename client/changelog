Changelog v0.8:
• Functions to check rarity type and change classes on render
• Function to find item type searched by the player in the players selected character
• New classes for all rarity types
• Added a dev button that console logs current important states and props in each component

Changelog v0.8b:
• Added entirely new window to select item and render that instead of searched item for more usage
• Added function to check if item has implicit/explicit mods or not and render or not the implicits (caused render crash)
• Added only 1 render of <hr> tag if item does not have implicits
